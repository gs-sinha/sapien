Dispatch is the process of matching an available rider to a ready order once
allocation completes.

## Dispatch Overview

Dispatch Notes
--------------

The two lines above look like a setext heading, but sapien does not support
underline headings, so this text stays inside "Dispatch Overview".

### Examples

Some notes about retry timing.

## Troubleshooting

### Examples

Different examples, under a different parent, to exercise duplicate-heading
handling.

```yaml
# this is a YAML comment, not a markdown heading
retries: 3
```
