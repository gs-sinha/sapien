# invalid: "parallel" is a reserved future key (parallel step groups) and is rejected today
version: 1
id: order-allocation
steps:
  - id: create
    call: order-service.createOrder
    parallel: true
    assert:
      - status == 201
