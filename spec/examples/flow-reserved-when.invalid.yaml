# invalid: "when" is a reserved future key (conditional steps) and is rejected today
version: 1
id: order-allocation
steps:
  - id: create
    call: order-service.createOrder
    when: "${inputs.city == 'NYC'}"
    assert:
      - status == 201
